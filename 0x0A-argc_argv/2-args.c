#include <stdio.h>

/**
 * main - prints all arguments, one per line
 * @argc: arguments count
 * @argv: array of arguments
 * Return: always 0
 */

int main(int argc, char *argc[])
{
	for (argv[] = 0; argv[] <= argc - 1)
	{
		printf("%s\n", argv[]);
		argv[]++;
	}
}
