#include <stdio.h>
/**
 * main - entry point
 * argc: argument count
 * *argv: argument vector
 * Return: 0
 */
int main (int argc, char *argv[])
{
	for (argv[] = 0; argv[] <= (argc - 1); argv[]++)
	{
		_putchar("argv[0]");
	}
	_putchar('\n');
}
