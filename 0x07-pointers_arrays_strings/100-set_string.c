#include "main.h"
/**
 * set_string - sets the value of a pointer to a char
 * @s: pointer
 * @to: char to which pointer is being assigned
 * Return: no return
 */
void set_string(char **s, char *to)
{
	*s = to;
}
